procedure err3 is
	procedure A( r ) is
		integer x;
		x := r[0];
	end
	procedure A( r ) is
		integer x;
		x := r[0];
	end
	array a;
begin
	a := new integer[1];
	a[0] := 5;
	begin A(a);
end
